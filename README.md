# LogConverter
Converts logs from chats or message boards from .txt files into .docs with basic formating. Parses some common BBCode-Tags.

To use it, place the .txt you wish to convert in the same directory as the .jar and execute it. 
The generated .doc should appear in the same directory. 
